class Note {
  int id;
  String title;
  String content;
  DateTime createDate;

  Note(
      {required this.id,
      required this.title,
      required this.content,
      required this.createDate,});
}